gitea:
  admin:
    username: "admin"
    password: "1"
    email: "admin@example.com"

service:
  http:
    type: NodePort
    port: 3000

persistence:
  enabled: true
  storageClass: "standard"
  size: 10Gi

